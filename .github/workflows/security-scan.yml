name: security-scan
on: [push, pull_request]
jobs:
  scan-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - run: echo "Run tflint, checkov, trivy, gitleaks in hardened CI with approved policies"
