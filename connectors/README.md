# Connectors
Each product directory has mock and real skeleton adapters. Real adapters read endpoints and tokens from environment variables only, use timeouts, and keep external API calls behind adapter interfaces.

QRadar sender supports dry-run JSON syslog and LEEF payload generation.
