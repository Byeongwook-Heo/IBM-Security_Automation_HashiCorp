apiVersion: v1
kind: Secret
metadata:
  name: enterprise-registry-pull-secret
  namespace: security-lab
type: kubernetes.io/dockerconfigjson
# HUMAN REVIEW REQUIRED: create this secret out-of-band with approved enterprise registry credentials.
data:
  .dockerconfigjson: BASE64_ENCODED_DOCKER_CONFIG_PLACEHOLDER
