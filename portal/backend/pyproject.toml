[project]
name = "security-portal-backend"
version = "0.1.0"
dependencies = ["fastapi>=0.111", "uvicorn>=0.30", "pydantic>=2.7", "httpx>=0.27", "pytest>=8", "python-dotenv>=1"]
[tool.pytest.ini_options]
pythonpath = ["."]
